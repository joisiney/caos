import React from 'react';
import { StyleSheet, View, Text } from 'react-native';

export interface <%- nameCapitalized %>ComponentProps {
  children?: React.ReactNode;
  testID?: string;
}

export const <%- nameCapitalized %>Component: React.FC<<%- nameCapitalized %>ComponentProps> = ({
  children,
  testID = '<%- name %>-component',
}) => {
  return (
    <View style={styles.container} testID={testID}>
      <Text style={styles.title}><%- nameCapitalized %> Component</Text>
      {children}
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 16,
  },
  title: {
    fontSize: 18,
    fontWeight: 'bold',
    marginBottom: 16,
  },
});

// Export default para facilitar imports
export default <%- nameCapitalized %>Component; 