import React from 'react';
import { View, StyleSheet } from 'react-native';
import { use<%- nameCapitalized %>Repository } from '@/repositories/<%- name %>.repository';

export interface <%- nameCapitalized %>FeatureProps {
  testID?: string;
}

export const <%- nameCapitalized %>Feature: React.FC<<%- nameCapitalized %>FeatureProps> = ({
  testID = '<%- name %>-feature',
}) => {
  const <%- name %>Repository = use<%- nameCapitalized %>Repository();

  const handleAction = async () => {
    try {
      // Implementar lógica da feature aqui
      console.log('<%- nameCapitalized %> feature action');
    } catch (error) {
      console.error('Erro na feature <%- name %>:', error);
    }
  };

  return (
    <View style={styles.container} testID={testID}>
      {/* Implementar UI da feature aqui */}
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});

export default <%- nameCapitalized %>Feature; 