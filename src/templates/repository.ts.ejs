import { findOne<%- nameCapitalized %>Gateway } from '@/gateways/find-one-<%- name %>.gateway';
import { <%- nameCapitalized %>Model } from '@/models/<%- name %>.model';

export const use<%- nameCapitalized %>Repository = () => {
  const findOneById = async (id: string) => {
    const data = await findOne<%- nameCapitalized %>Gateway({ id });
    return new <%- nameCapitalized %>Model(data);
  };

  return { findOneById };
}; 