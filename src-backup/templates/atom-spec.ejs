import { render } from '@testing-library/react-native';
import { <%= nameCapitalized %>Atom } from './<%= name %>.atom';

const Hoc<%= nameCapitalized %>Atom = () => <<%= nameCapitalized %>Atom testID="<%= name %>-atom" />;

describe('Atom: <<%= nameCapitalized %>Atom/>', () => {
  it('deve renderizar corretamente', () => {
    const { getByTestId } = render(<<%= nameCapitalized %>Atom testID="<%= name %>" />);
    expect(getByTestId('<%= name %>-atom')).toBeTruthy();
  });
}); 