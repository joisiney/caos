import { FC } from 'react';
import { View } from 'react-native';
import { N<%= nameCapitalized %>Atom } from './<%= name %>.type';

export const <%= nameCapitalized %>Atom: FC<N<%= nameCapitalized %>Atom.Props> = ({ testID }) => {
  return (
    <View
      testID={`${testID}-atom`}
      className="w-10 h-10 bg-purple-400"
    />
  );
}; 